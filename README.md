# flutter_handling_memory

## Handling the requests MOBX
 
 - When the user have already sent a request and the list is already in memory, I verify and don't sent another request due mobx management state.
